# 건넴 개발 TODO

기준 문서: `PRD.md`, `docs/00-working-rules.md`부터 `docs/05-development-plan.md`까지.

상태 규칙:

- `[ ]` 예정 또는 미검증
- `[x]` 구현과 요구 검증이 모두 완료됨
- 진행 중인 작업은 항목 끝에 `(진행 중)` 표시
- 환경 문제로 검증하지 못하면 완료 처리하지 않음

## 0. 기획 및 작업 기반

- [x] PRD 요구사항을 구현 단위로 분해한다.
- [x] 상시 작업 규칙 문서를 작성한다.
- [x] 제품 범위와 사용자 흐름 문서를 작성한다.
- [x] 아키텍처·상태·컴포넌트 규칙 문서를 작성한다.
- [x] UI·콘텐츠·반응형 규칙 문서를 작성한다.
- [x] 테스트 및 완료 정의 문서를 작성한다.
- [x] 단계별 개발 계획을 작성한다.
- [x] `AGENTS.md`에 문서 색인과 코드 매핑을 작성한다.

## 1. 프로젝트 기반

- [x] Vite React 프로젝트를 구성한다.
- [x] React, React DOM, React Router DOM 6 의존성을 설정한다.
- [x] `dev`, `build`, `preview` npm 스크립트를 설정한다.
- [x] `index.html`, `src/main.jsx`, `src/styles.css`를 연결한다.
- [x] pages, components, data, utils 폴더 구조를 만든다.
- [x] 최소 앱의 프로덕션 빌드를 통과시킨다.

## 2. 상태와 라우팅

- [x] `initialCardData` 전체 필드를 정의한다.
- [x] App에서 부분 병합 상태 업데이트를 구현한다.
- [x] 버튼 및 결과 문구 fallback 유틸을 구현한다.
- [x] `/`, `/create`, `/show`, `/result/accept`, `/result/reject`를 연결한다.
- [x] wildcard 경로를 홈으로 replace redirect한다.
- [x] 페이지 이동 중 카드 상태가 유지되는지 확인한다.
- [x] 카드 작성 데이터를 localStorage에 자동 저장하고 새로고침 시 복구한다.
- [x] 홈을 거쳐 작성창에 다시 진입해도 React 상태와 localStorage 저장 데이터를 유지한다.

## 3. 공통 UI 컴포넌트

- [x] `CardFrame` 모바일 프레임을 구현한다.
- [x] `MainQuestionCard`의 조건부 이미지와 문구 표시를 구현한다.
- [x] `ActionButtons` 수락·거절 버튼을 구현한다.
- [x] `ImageUploader` 선택, data URL 변환, 재선택, 삭제를 구현한다.
- [x] `ImageBlock`의 이미지 조건부 렌더링을 구현한다.
- [x] `ResultMessage`를 구현한다.
- [x] `ToggleSwitch`와 접근성 상태를 구현한다.
- [x] `ReplyInput`의 preview 모드와 공백 검증을 구현한다.
- [x] `ReplyCard`의 조건부 표시를 구현한다.

## 4. 작성자 흐름

- [x] HomePage 소개와 로그인 없이 사용 안내를 구현한다.
- [x] 카드 만들기에서 저장 상태를 유지한 채 `/create`로 이동한다.
- [x] CreateCardPage의 3단계 탭을 구현한다.
- [x] 이전·다음·최종 보여주기 동작을 구현한다.
- [x] 메인 카드 문구, 이미지, 버튼 문구 편집을 구현한다.
- [x] 수락 결과 문구, 이미지, 답장 토글과 미리보기를 구현한다.
- [x] 거절 결과 문구, 이미지, 답장 토글과 미리보기를 구현한다.
- [x] 중간 이동에는 검증하지 않고 최종 이동에서만 메인 문구를 검증한다.
- [x] 메인 문구 오류가 첫 단계에 표시되는지 확인한다.

## 5. 응답자 흐름

- [x] ShowCardPage에 메인 카드와 수락·거절 버튼을 표시한다.
- [x] ShowCardPage 뒤로가기를 `/create`에 연결한다.
- [x] 수락·거절 버튼을 각 결과 경로에 연결한다.
- [x] AcceptResultPage의 이미지, 결과 문구, 답장 조건부 표시를 구현한다.
- [x] RejectResultPage의 이미지, 결과 문구, 답장 조건부 표시를 구현한다.
- [x] 결과별 답장 제출 상태를 독립적으로 관리한다.
- [x] 수락·거절 결과 페이지에 현재 결과만 지우는 원형 답변 초기화 버튼을 추가한다.
- [x] 제출 답장을 입력 폼 위 ReplyCard로 표시한다.
- [x] 결과 화면 뒤로가기를 `/show`에 연결한다.

## 6. 스타일과 반응형

- [x] 최대 430px 데스크톱 모바일 프레임을 구현한다.
- [x] 420px 이하 전체 화면 전환을 구현한다.
- [x] 기본 녹색, 수락 파란색, 거절 붉은색 맥락을 적용한다.
- [x] 긴 문구와 버튼 문구의 안전한 줄바꿈을 적용한다.
- [x] 이미지 `object-fit: contain`과 조건부 공간 제거를 확인한다.
- [ ] 터치 영역, focus 표시, 아이콘 버튼 aria-label을 확인한다.

## 7. 인수 및 회귀 검증

- [x] `npm run build`가 성공한다.
- [ ] 홈부터 수락 결과와 답장까지 전체 흐름을 검증한다.
- [ ] 홈부터 거절 결과와 답장까지 전체 흐름을 검증한다.
- [ ] 버튼과 결과 fallback의 빈 값·공백 값을 검증한다.
- [ ] 세 이미지의 선택·재선택·삭제·빈 상태를 검증한다.
- [ ] 수락·거절 답장 토글과 상태 독립성을 검증한다.
- [ ] 320px, 390px, 데스크톱 레이아웃을 검증한다.
- [ ] 키보드 흐름, focus, aria-label을 검증한다.
- [ ] 모든 사용자 노출 한글이 UTF-8로 정상 표시되는지 확인한다.
- [ ] `PRD.md` 11절의 모든 인수 조건을 최종 확인한다.

## 결정 기록

- 2026-07-13: 사용자 요청에 따라 단일 세션 범위를 확장해 원본 `cardData`를 `gunnam.cardData.v1` 키로 자동 저장·복구한다. 홈의 카드 만들기는 기존 작성 데이터를 유지하고 이어서 편집한다.
- 2026-07-10: 현재 저장소는 문서만 존재하므로 코드 관련 항목은 모두 미완료 상태에서 시작한다.
- 2026-07-10: 개발 순서는 기반 → 상태·라우팅 → 공통 UI → 작성자 흐름 → 응답자 흐름 → 품질 검증으로 정한다.
- 2026-07-10: 프로젝트 기반과 상태·라우팅 골격을 구현하고 Vite 프로덕션 빌드를 통과했다. 화면은 다음 단계에서 공통 컴포넌트로 교체할 임시 최소 UI다.
- 2026-07-10: 공통 UI 컴포넌트 9개와 모바일 프레임·반응형 기반을 구현하고 빌드를 통과했다. 이미지와 답장 컴포넌트의 실제 화면 통합 검증은 작성자·응답자 흐름 단계에서 수행한다.
- 2026-07-10: CreateCardPage의 3단계 편집형 미리보기, 자유 이동, 결과별 독립 토글과 최종 메인 문구 검증을 구현하고 빌드를 통과했다.
- 2026-07-10: ShowCardPage와 수락·거절 결과, 결과별 독립 답장 제출 및 뒤로가기 흐름을 구현하고 빌드를 통과했다. 다음 단계는 실제 브라우저 인수·반응형 검증이다.
- 2026-07-10: 320px 입력 오버플로를 제거하고, 봉투 로고 기준의 부드러운 녹색·파란색·산호색 팔레트와 결과 카드·홈 브랜드 영역을 통일했다. 앱 아이콘은 흐림 없는 평면 스타일로 재제작하고 OG 이미지 균형을 조정했으며 빌드를 통과했다.
