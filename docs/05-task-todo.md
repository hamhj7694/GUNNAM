# 작업 단위별 TODO

## 0. 프로젝트 준비

- [x] React + Vite 프로젝트를 구성한다.
- [x] `npm install`을 완료한다.
- [ ] `npm run dev`로 개발 서버가 실행되는지 확인한다.
- [x] 기본 폴더 구조를 만든다.
- [x] 전역 CSS 초기값과 모바일 우선 레이아웃 기준을 정한다.

권장 구조:

```text
src/
  components/
  pages/
  data/
  utils/
  App.jsx
  main.jsx
  styles.css
```

## 1. 상태와 라우팅

- [x] `initialCardData`를 정의한다.
- [x] `mainImage`를 포함한 `initialCardData`를 정의한다.
- [x] App 상위에서 `cardData`, `setCardData` state를 관리한다.
- [x] React Router를 설치하거나, 프로젝트 규모에 맞게 간단한 화면 상태 라우팅을 구성한다.
- [x] `/`, `/create`, `/show`, `/result/accept`, `/result/reject` 흐름을 만든다.
- [x] 표시용 파생값 계산 유틸을 만든다.

완료 기준:

- [x] 각 페이지로 이동할 수 있다.
- [x] 페이지 이동 후에도 cardData가 유지된다.
- [x] 새로고침 복구는 구현하지 않는다.

## 2. 공통 레이아웃과 UI 기반

- [x] `CardFrame`을 만든다.
- [x] 모바일 화면 폭, 중앙 정렬, 기본 여백을 잡는다.
- [x] 버튼 기본 스타일을 만든다.
- [x] 입력 필드, textarea, 섹션 제목 스타일을 정리한다.
- [x] 에러 메시지 스타일을 만든다.

완료 기준:

- [ ] 모바일과 데스크톱에서 화면 폭이 안정적이다.
- [ ] 텍스트와 버튼이 겹치지 않는다.

## 3. HomePage

- [x] 서비스명 `건넴`을 표시한다.
- [x] 짧은 설명 문구를 작성한다.
- [x] `카드 만들기` 버튼을 만든다.
- [x] `로그인 없이 바로 사용` 안내를 표시한다.
- [x] 카드 만들기 클릭 시 빈 데이터로 CreateCardPage 이동한다.

완료 기준:

- [ ] 시작 버튼이 정상 동작한다.

## 4. CreateCardPage

- [x] 메인 카드 안에서 직접 쓰는 textarea를 만든다.
- [x] 메인 카드 이미지 업로드를 만든다.
- [x] 수락 버튼 문구 input을 만든다.
- [x] 거절 버튼 문구 input을 만든다.
- [x] 수락 결과 문구를 카드 안에서 직접 수정할 수 있다.
- [x] 거절 결과 문구를 카드 안에서 직접 수정할 수 있다.
- [x] 수락 후 답장 입력 ToggleSwitch를 연결한다.
- [x] 거절 후 답장 입력 ToggleSwitch를 연결한다.
- [x] 수락 후 이미지/QR ImageUploader를 연결한다.
- [x] 거절 후 이미지 ImageUploader를 연결한다.
- [x] 3단계 작성 흐름을 만든다.
- [x] 마지막 단계에 `상대에게 보여주기` 버튼을 만든다.
- [x] `mainText`가 비어 있으면 이동을 막고 에러를 표시한다.

완료 기준:

- [ ] 모든 입력값이 cardData에 반영된다.
- [ ] 필수값 검증이 동작한다.

## 5. ImageUploader

- [x] 이미지 파일 선택 기능을 만든다.
- [x] 선택 이미지 미리보기를 만든다.
- [x] 다시 선택 기능을 만든다.
- [x] 삭제 기능을 만든다.
- [x] 적용하기 기능을 만든다.
- [x] 수락 후 이미지와 거절 후 이미지에서 재사용한다.

완료 기준:

- [ ] 서버 업로드 없이 React state에 이미지가 보관된다.
- [ ] 삭제하면 결과 화면에서 이미지 영역이 사라진다.

## 6. 3단계 작성형 미리보기

- [x] 단계 상태를 만든다.
- [x] `메인 카드` 단계를 만든다.
- [x] `수락 페이지` 단계를 만든다.
- [x] `거절 페이지` 단계를 만든다.
- [x] 이전/다음 버튼을 만든다.
- [x] `상대에게 보여주기` 버튼을 만든다.
- [x] 결과 메시지 fallback 규칙을 적용한다.
- [x] 이미지 조건부 렌더링을 적용한다.
- [x] 답장 입력 ON일 때만 수락 페이지 단계에 ReplyInput을 표시한다.

완료 기준:

- [ ] 작성 단계가 실제 표시 규칙과 동일하게 보인다.

## 7. ShowCardPage

- [x] MainQuestionCard를 사용해 메인 문구를 크게 표시한다.
- [x] ActionButtons를 사용해 수락/거절 버튼을 표시한다.
- [x] 수락 클릭 시 AcceptResultPage로 이동한다.
- [x] 거절 클릭 시 RejectResultPage로 이동한다.
- [x] 수정, 홈, 뒤로가기, 설정, 공유 등 불필요한 UI를 제거한다.

완료 기준:

- [ ] 상대가 선택해야 할 두 버튼 외 방해 요소가 없다.

## 8. AcceptResultPage

- [x] ImageBlock으로 수락 후 이미지/QR을 표시한다.
- [x] ResultMessage로 수락 결과 메시지를 표시한다.
- [x] `acceptReplyEnabled`가 true일 때만 ReplyInput을 표시한다.
- [x] 빈 답장 제출을 막는다.
- [x] 빈 답장 제출 시 에러를 표시한다.
- [x] 수락 답장 제출 시 `acceptReplyText`, `acceptReplySubmitted`를 업데이트한다.
- [x] `acceptReplySubmitted`가 true이면 ReplyCard를 표시한다.

완료 기준:

- [ ] 답장 입력 OFF/ON 케이스가 모두 정상 동작한다.
- [ ] 답장 제출 후 카드가 표시된다.

## 9. RejectResultPage

- [x] ImageBlock으로 거절 후 이미지를 표시한다.
- [x] ResultMessage로 거절 결과 메시지를 표시한다.
- [x] `rejectReplyEnabled`가 true일 때만 ReplyInput을 표시한다.
- [x] 거절 답장 제출 후 ReplyCard를 표시한다.
- [x] 거절 사유, 다시 묻기, 이유 선택, 통계 저장 UI를 만들지 않는다.

완료 기준:

- [ ] 거절 흐름이 짧고 깔끔하게 끝난다.

## 10. 모바일 UI polish

- [ ] 320px 폭에서 레이아웃을 확인한다.
- [ ] 390px 폭에서 레이아웃을 확인한다.
- [ ] 데스크톱에서 중앙 모바일 프레임을 확인한다.
- [ ] 버튼 터치 영역을 충분히 확보한다.
- [ ] 긴 버튼 문구와 긴 카드 문구를 확인한다.
- [ ] 이미지가 큰 경우에도 화면이 무너지지 않게 한다.
- [ ] 카드 전환 애니메이션을 필요 최소한으로 추가한다.

완료 기준:

- [ ] 모바일에서 자연스럽게 사용할 수 있다.

## 11. 최종 검수

- [ ] `npm run dev` 상태에서 전체 흐름을 처음부터 끝까지 테스트한다.
- [ ] 빈 메인 문구 검증을 확인한다.
- [ ] 수락 버튼 기본값 `좋아요`를 확인한다.
- [ ] 거절 버튼 기본값 `싫어요`를 확인한다.
- [ ] 수락 결과가 수락 버튼 문구로 표시되는지 확인한다.
- [ ] 거절 결과가 거절 버튼 문구로 표시되는지 확인한다.
- [ ] 이미지 없음 조건부 렌더링을 확인한다.
- [ ] 수락/거절 답장 입력 ON/OFF를 확인한다.
- [ ] ShowCardPage의 불필요한 UI 부재를 확인한다.
- [ ] 거절 결과 화면에 거절 사유 입력이나 다시 묻기 흐름이 없는지 확인한다.
